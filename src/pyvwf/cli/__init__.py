"""Console-script entry points for the core PyVWF workflow.

Installed as ``pyvwf-validate``, the harness's train, evaluate and transfer
verbs; see ``[project.scripts]`` in ``pyproject.toml``.
:mod:`pyvwf.cli.common` holds what every entry point shares.
"""
