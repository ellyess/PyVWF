"""Console-script entry points for the core PyVWF workflow.

Installed as ``pyvwf-train`` (the legacy ``PyVWF`` path) and ``pyvwf-validate``
(the validation harness); see ``[project.scripts]`` in ``pyproject.toml``.
:mod:`vwf.cli.common` holds what every entry point shares.
"""
