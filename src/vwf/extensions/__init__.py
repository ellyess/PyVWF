"""Optional PyVWF extensions, kept out of the default import path.

Each subpackage needs dependencies the core install does not carry, declared as
an extra in ``pyproject.toml``. Importing ``vwf`` does not import these.
"""
